<dl class="entry-meta-list">
	<dt>Tagged:</dt>
	<%- resource.tags.sort_by { |tag| tag.name.downcase }.each do |tag| -%>
		<dd><%= link_to tag.name, tag_path(tag), class: 'p-category' %></dd>
	<%- end -%>
</dl>