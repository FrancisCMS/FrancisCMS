<% if @links.empty? %>
	<p>No links found!</p>
<% else %>
	<ol>
		<% @links.each do |link| %>
			<li><%= link_to link.title, link_path(link.id) %></li>
		<% end %>
	</ol>
<% end %>

<h1>Add a new link</h1>

<form action="<%= links_path %>" method="post">
	<%= partial :'links/form' %>
</form>