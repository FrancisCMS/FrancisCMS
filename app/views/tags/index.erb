<header>
	<h1>Tags</h1>
</header>

<% if @tags.any? %>
	<ol>
		<% @tags.each do |tag| %>
			<li><%= link_to tag.name, tag_path(tag.slug) %></li>
		<% end %>
	</ol>
<% else %>
	<p>No tags found!</p>
<% end %>